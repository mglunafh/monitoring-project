server:
  port: 8080
  shutdown: graceful

spring:
  datasource:
    url:
    username:
    password:

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  metrics:
    export:
      prometheus:
        enabled: true

### for initializing initial database structure
#  jpa:
#    generate-ddl: true
#    defer-datasource-initialization: true
#    hibernate:
#      ddl-auto:
#        sql: create
#    init:
#      mode: always
#    properties:
#      jakarta:
#        persistence:
#          create-database-schemas: true
#      hibernate:
#        default_schema: delivery
